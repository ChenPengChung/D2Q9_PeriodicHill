# macOS resource forks (always ignore everywhere)
.DS_Store
._*
*.vtk
**/._*

# IDE (保留 settings.json)
# .vscode/

# Build outputs
*.o
*.out
*.exe
*.dat
*.log
main

# Output directory - ignore VTK and PNG images, but keep GIF animations
output/*.vtk
output/*.dat
output_images/**/*.png
!output_images/*.gif

# Explicitly ignore macOS resource forks in figures
output/figures/._*

*.dSYM/
# CUDA compiled
*.cubin
*.ptx

# Git internal junk (macOS resource forks in .git are handled separately)
.git/objects/pack/._*
