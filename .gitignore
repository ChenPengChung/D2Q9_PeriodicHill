# macOS resource forks (always ignore everywhere)
.DS_Store
._*
*.vtk
**/._*

# IDE (保留 settings.json)
# .vscode/

# Build outputs
*.o
*.out
*.exe
*.dat
*.log
main

# Output directory - ignore VTK but keep figures (not ._* files)
output/*.vtk
output/*.dat
!output/figures/
!output/figures/*.png
# Explicitly ignore macOS resource forks in figures
output/figures/._*

*.dSYM/
# CUDA compiled
*.cubin
*.ptx

# Git internal junk (macOS resource forks in .git are handled separately)
.git/objects/pack/._*
